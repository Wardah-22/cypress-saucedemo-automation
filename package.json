{
  "name": "cypress-saucedemo-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "npx cypress run",
    "merge:reports": "npx mochawesome-merge cypress/results/*.json > cypress/results/output.json",
     "generate:report": "npx marge cypress/results/output.json --reportDir cypress/results --reportFilename Cypress-Report --inline",
    "report": "npm run cy:run && npm run merge:reports && npm run generate:report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^14.5.3"
  },
  "devDependencies": {
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0"
  }
}
